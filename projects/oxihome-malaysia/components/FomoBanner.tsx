// FomoBanner stub for oxihome-malaysia.
//
// The legacy oxihome design does not show a FOMO promo strip — the visual
// identity is built around the teal Oxi.home brand without urgency banners.
// This component is kept as a no-op so the homepage + location page can
// import + render `<FomoBanner />` (satisfying the shared-shell checklist)
// without disrupting the restored layout.
export default function FomoBanner() {
  return null;
}
