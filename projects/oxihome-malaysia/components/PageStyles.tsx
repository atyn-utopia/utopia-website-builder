// PageStyles stub for oxihome-malaysia.
//
// The legacy oxihome layout ships its own stylesheet (app/globals.css) with
// the teal brand palette. The shared PageStyles block (used by other
// Webcore sites like sewa-excavator) would collide with the existing
// styles, so this stub renders nothing. Pages still import + render
// `<PageStyles />` to satisfy the shared-shell checklist.
export default function PageStyles() {
  return null;
}
