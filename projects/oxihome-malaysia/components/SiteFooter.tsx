// SiteFooter stub for oxihome-malaysia. See SiteHeader for context — the
// legacy oxihome layout renders its bespoke footer inline inside
// app/[locale]/layout.tsx, so this stub returns null and exists only to
// keep the import + render present on the homepage + location page.
export default function SiteFooter(_props: { locale?: string }) {
  return null;
}
